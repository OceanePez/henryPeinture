/** @type {import('next-intl').NextIntlConfig} */
module.exports = {
  locales: ["fr", "en"], // liste des langues supportées
  defaultLocale: "fr", // langue par défaut
  // Optionnel : définir les namespaces utilisés
  // namespaces: ['common'],
};
